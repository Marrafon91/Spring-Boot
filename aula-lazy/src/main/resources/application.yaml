spring:
  application:
    name: aula-lazy

  profiles:
    active: test

---
# TEST PROFILE
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:aula-lazy
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
#    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true